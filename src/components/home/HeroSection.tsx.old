import Link from "next/link";

export default function HeroSection() {
  return (
    <section
      className="w-full bg-white border-b border-gray-200"
      aria-labelledby="hero-heading"
    >
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-14 md:py-20">
        <div className="max-w-3xl text-center mx-auto md:text-left md:mx-0">

          {/* Primary Heading — Only H1 on this page */}
          <h1
            id="hero-heading"
            className="text-3xl md:text-4xl font-bold text-gray-900 leading-snug"
            style={{ fontFamily: "var(--font-bangla)" }}
          >
            বেফাকুল মাদারিসিল আরাবিয়া বাংলাদেশ
          </h1>

          {/* Tagline */}
          <p
            className="mt-4 text-lg md:text-xl text-gray-700 leading-relaxed"
            style={{ fontFamily: "var(--font-bangla)" }}
          >
            বাংলাদেশের কওমি মাদরাসা শিক্ষাব্যবস্থার সর্বোচ্চ তত্ত্বাবধায়ক বোর্ড
          </p>

          {/* Supporting Description */}
          <p
            className="mt-4 text-base text-gray-600 leading-relaxed"
            style={{ fontFamily: "var(--font-bangla)" }}
          >
            বেফাক পরিচালিত পরীক্ষার ফলাফল, নোটিশ, এবং শিক্ষাপ্রতিষ্ঠান
            সংক্রান্ত সকল তথ্য এই ওয়েবসাইটে পাওয়া যাবে।
          </p>

          {/* CTA Buttons */}
          <div className="mt-8 flex flex-col sm:flex-row gap-3 justify-center md:justify-start">

            {/* Primary CTA */}
            <Link
              href="/results"
              className="inline-block px-6 py-3 rounded text-base font-medium text-white text-center focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2"
              style={{
                backgroundColor: "var(--color-primary)",
                outlineColor: "var(--color-primary)",
              }}
            >
              ফলাফল দেখুন
            </Link>

            {/* Secondary CTA */}
            <Link
              href="/notices"
              className="inline-block px-6 py-3 rounded text-base font-medium text-center border-2 bg-white text-gray-800 hover:bg-gray-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2"
              style={{
                borderColor: "var(--color-primary)",
                color: "var(--color-primary)",
                outlineColor: "var(--color-primary)",
              }}
            >
              সকল নোটিশ
            </Link>

          </div>
        </div>
      </div>
    </section>
  );
}